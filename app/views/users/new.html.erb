<h1>Sign Up!</h1>

<%= form_for User.new do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<%= f.label :password %>
	<%= f.password_field :password_confirmation %>
	<%= f.submit :'Create Account' %>
<% end %>